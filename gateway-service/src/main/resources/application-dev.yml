auth:
  service:
    uri: ${AUTH_SERVICE_URI:http://localhost:9000}

marketplace:
  service:
    uri: ${MARKETPLACE_SERVICE_URI:http://localhost:8081}

payment:
  service:
    uri: ${PAYMENT_SERVICE_URI:http://localhost:8082}

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
