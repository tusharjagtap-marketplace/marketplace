server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://${DEV_DB_HOST:marketplacedb-dev.cvoiyseiupjx.ap-south-1.rds.amazonaws.com}:3306/marketplacedb-dev?createDatabaseIfNotExist=true
    username: ${DEV_DB_USER:admin}
    password: ${DEV_DB_PASSWORD:Ajinkyahb2026}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
 
