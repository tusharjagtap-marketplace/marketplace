apiVersion: v1
kind: ConfigMap
metadata:
  name: microservices-config
data:
  spring.profiles.active: "dev" # Deployment environment defaults to dev
  db.host: "mysql-db"
  db.port: "3306"
  db.name: "marketplace_db"
  auth.service.host: "auth-service"
  marketplace.service.host: "marketplace-service"
  payment.service.host: "payment-service"
